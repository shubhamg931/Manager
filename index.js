import { AppRegistry } from 'react-native';
import App from './src/App';

// YellowBox.ignoreWarnings(['Warning: isMounted(...) is deprecated', 'Module RCTImageLoader', 'Setting a timer for a long period']);
AppRegistry.registerComponent('manager', () => App);
